"""
tools/filesystem.py

Filesystem tools available to the agent.
Each function corresponds to a tool defined in tools/schema.py.
Full implementations added on Day 5.
"""


def read_file(path: str) -> str:
    """Read and return the contents of a file."""
    # Temporary stub — real implementation on Day 5
    return f"[stub] Would read file: {path}"


def list_dir(path: str) -> str:
    """List the contents of a directory."""
    # Temporary stub — real implementation on Day 5
    return f"[stub] Would list directory: {path}"


def write_file(path: str, content: str) -> str:
    """Write content to a file."""
    # Temporary stub — real implementation on Day 5
    return f"[stub] Would write to file: {path}"
